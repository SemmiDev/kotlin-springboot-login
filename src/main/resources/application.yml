server:
  port: 9090

spring:
  thymeleaf:
    cache: false
  security:
    oauth2:
      client:
        registration:
#          github:
#            client-id: xxxxxxxxxxxxxxxxx
#            client-secret: xxxxxxxxxxxxxxxxx
          okta:
            client-id: xxx
            client-secret: xxx
            client-name: Okta
            provider:
              okta:
                issuer-uri: https://oktadomain/oauth2/default